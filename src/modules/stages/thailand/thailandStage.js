import BaseStage from '../../../common/baseStage.js';

export default class ThailandStage extends BaseStage {
    constructor(game, gameResolver, gameRejector){
      super(game, gameResolver, gameRejector);
    }
}
